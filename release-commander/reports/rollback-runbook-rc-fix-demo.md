# Rollback Runbook — 1.3.0

Auto-generated by IBM Bob (Release Commander). Undo steps:

- Revert migration `0001_create_orders.sql`: re-create `orders` (`0001_create_orders.sql` contains the DROP step).
- Revert migration `0002_add_users.sql`: re-create `users` (`0002_add_users.sql` contains the DROP step).
- Restore prod `DATABASE_URL` credentials from the secret manager if the placeholder was reverted.
